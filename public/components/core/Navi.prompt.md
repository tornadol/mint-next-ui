# Navi
Storefront top nav — 80px tall, glassy translucent white with 10px backdrop blur (`var(--surface-panel)`), thin `var(--border-nav)` line beneath. Sticky by default.

```jsx
<Navi active="Store" />
```
